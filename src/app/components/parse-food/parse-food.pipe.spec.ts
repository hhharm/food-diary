import { ParseFoodPipe } from './parse-food.pipe';

describe('ParseFoodPipe', () => {
  it('create an instance', () => {
    const pipe = new ParseFoodPipe();
    expect(pipe).toBeTruthy();
  });
});
